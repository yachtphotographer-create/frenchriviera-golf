# Server
PORT=3000
NODE_ENV=development
# IMPORTANT: Generate a strong random secret with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=generate-64-byte-random-hex-string-here-use-command-above

# Database
DATABASE_URL=postgresql://frenchrivieragolf:password@localhost:5432/frenchrivieragolf

# Email
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@frenchriviera.golf
SMTP_PASS=your-smtp-password
EMAIL_FROM=noreply@frenchriviera.golf

# App
APP_URL=http://localhost:3000
APP_NAME=French Riviera Golf

# GolfCourseAPI (free)
GOLF_API_KEY=your-api-key-from-golfcourseapi.com
