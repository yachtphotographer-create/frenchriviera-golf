<%- include('../layouts/main', { body: `
<div class="container static-page">
    <div class="page-header">
        <h1>${typeof htmlLang !== 'undefined' && htmlLang === 'fr' ? 'Contact' : 'Contact Us'}</h1>
    </div>

    <div class="page-content">
        <section class="content-section">
            <p>${typeof htmlLang !== 'undefined' && htmlLang === 'fr'
                ? 'Vous avez une question ou une suggestion ? Nous serions ravis de vous entendre.'
                : 'Have a question or suggestion? We\\'d love to hear from you.'}</p>
        </section>

        <section class="content-section">
            <div class="contact-grid">
                <div class="contact-card">
                    <div class="contact-icon">📧</div>
                    <h3>Email</h3>
                    <p><a href="mailto:hello@frenchriviera.golf">hello@frenchriviera.golf</a></p>
                </div>
                <div class="contact-card">
                    <div class="contact-icon">📍</div>
                    <h3>${typeof htmlLang !== 'undefined' && htmlLang === 'fr' ? 'Localisation' : 'Location'}</h3>
                    <p>${typeof htmlLang !== 'undefined' && htmlLang === 'fr' ? 'Côte d\\'Azur, France' : 'French Riviera, France'}</p>
                </div>
                <div class="contact-card">
                    <div class="contact-icon">📱</div>
                    <h3>Instagram</h3>
                    <p><a href="https://instagram.com/frenchrivieragolf" target="_blank" rel="noopener">@frenchrivieragolf</a></p>
                </div>
            </div>
        </section>

        <section class="content-section">
            <h2>${typeof htmlLang !== 'undefined' && htmlLang === 'fr' ? 'Questions fréquentes' : 'Common Questions'}</h2>

            <div class="faq-list">
                <div class="faq-item">
                    <h3>${typeof htmlLang !== 'undefined' && htmlLang === 'fr' ? 'Le service est-il gratuit ?' : 'Is the service free?'}</h3>
                    <p>${typeof htmlLang !== 'undefined' && htmlLang === 'fr'
                        ? 'Oui, French Riviera Golf est entièrement gratuit pour tous les golfeurs.'
                        : 'Yes, French Riviera Golf is completely free for all golfers.'}</p>
                </div>
                <div class="faq-item">
                    <h3>${typeof htmlLang !== 'undefined' && htmlLang === 'fr' ? 'Comment puis-je signaler un problème ?' : 'How do I report an issue?'}</h3>
                    <p>${typeof htmlLang !== 'undefined' && htmlLang === 'fr'
                        ? 'Envoyez-nous un email à hello@frenchriviera.golf avec les détails du problème.'
                        : 'Send us an email at hello@frenchriviera.golf with details of the issue.'}</p>
                </div>
                <div class="faq-item">
                    <h3>${typeof htmlLang !== 'undefined' && htmlLang === 'fr' ? 'Puis-je suggérer un nouveau parcours ?' : 'Can I suggest a new course?'}</h3>
                    <p>${typeof htmlLang !== 'undefined' && htmlLang === 'fr'
                        ? 'Absolument ! Contactez-nous avec les détails du parcours que vous aimeriez voir ajouté.'
                        : 'Absolutely! Contact us with details of the course you\\'d like to see added.'}</p>
                </div>
            </div>
        </section>
    </div>
</div>
` }) %>
